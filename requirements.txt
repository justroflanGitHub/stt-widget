faster-whisper>=1.0.0
PyQt5>=5.15.0
sounddevice>=0.4.6
numpy>=1.24.0
pyperclip>=1.8.2
pynput>=1.7.6
pytest>=7.0.0
# Pin: onnxruntime ≥1.30 crashes (0xC0000005 / DLL init failure) when imported
# after PyQt5 in the same process.
onnxruntime==1.26.0
# cuBLAS runtime for ctranslate2's CUDA backend on Windows (CTranslate2 4.8.x
# is built against CUDA 12.4 and loads cublas64_12.dll lazily; without it GPU
# inference deadlocks). build.spec places these DLLs at the app root.
nvidia-cublas-cu12==12.4.5.8; sys_platform == "win32"
